package com.childInHelp2026.app

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity

/**
 * Empty activity to handle Health Connect permissions rationale.
 * In a real app, this should explain why the app needs these permissions.
 */
class HealthConnectPermissionsActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        // You can add a view explaining why you need Health Connect data here
        finish()
    }
}
